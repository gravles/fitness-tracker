'use client';

// Kinetic redesign: the bottom nav is now the floating glass pill.
// Kept under the original name so AuthWrapper's import stays stable.
export { KineticNav as BottomNav } from '@/components/kinetic/KineticNav';
